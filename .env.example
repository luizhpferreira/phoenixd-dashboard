# =============================================================================
# Phoenixd Dashboard - Environment Variables
# =============================================================================
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# Leave empty for mainnet, set to "testnet" for testnet
PHOENIXD_CHAIN=

# Phoenixd API password (auto-read from phoenix.conf by setup.sh)
# Only set manually if you know what you're doing
# PHOENIXD_PASSWORD=

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
POSTGRES_USER=phoenixd
POSTGRES_PASSWORD=phoenixd_secret
POSTGRES_DB=phoenixd_dashboard

# -----------------------------------------------------------------------------
# API URLs (Important for remote access!)
# -----------------------------------------------------------------------------
# If accessing the dashboard from a different machine (not localhost),
# you MUST set these to your server's IP or hostname.
#
# Examples:
#   Local only:      http://localhost:4001 (default)
#   Local network:   http://192.168.1.100:4001
#   Domain:          https://phoenixd.example.com:4001
#   Tailscale:       (auto-detected, no need to configure)
#
# NEXT_PUBLIC_API_URL=http://localhost:4001
# NEXT_PUBLIC_WS_URL=ws://localhost:4001

# Frontend URL (for CORS)
# FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Tailscale VPN (Optional - for secure remote access)
# -----------------------------------------------------------------------------
# Get your auth key from: https://login.tailscale.com/admin/settings/keys
# You can also configure this in the Settings page of the dashboard.
#
# TS_AUTHKEY=tskey-auth-xxxxx
# TS_HOSTNAME=phoenixd-dashboard

# -----------------------------------------------------------------------------
# Development
# -----------------------------------------------------------------------------
# NODE_ENV=development
